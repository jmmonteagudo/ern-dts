# Placeholder — abapGit poblará esta carpeta automáticamente cuando hagas
# "Stage and Push" desde ADT.
#
# Cada objeto ABAP (CDS view, behavior, service, etc.) genera 1-3 archivos:
#   ZI_ERN_PRODUCT.ddls.asddls    (DDIC source)
#   ZI_ERN_PRODUCT.ddls.xml       (metadatos)
#
# Folder logic: PREFIX (cada objeto va a una subcarpeta por su prefijo).
